<%- include('../layout/header.ejs') %>
    <link rel="stylesheet" href="/stylesheets/userStyles/userSignIn.css" type="text/css">
    <link rel="stylesheet" href="/stylesheets/userStyles/productPage.css" type="text/css">
    </head>

    <body>
        <div>
            <%- include('../layout/navIn.ejs') %>
            <%- include('../layout/categoryBar.ejs') %>
        </div>
        <div class="container topMargin">
            <h3>Product details</h3>
        </div>
        <div class="row row-cols-1 row-cols-md-3 g-4 m-5 gap-5 d-flex">
            <a href="/user/products/<%= product._id %>">
                <div class=" hover">
                    <div class="card">
                        <div class="d-flex align-items-center justify-content-center imageDiv">
                            <img src="/images/<%=product.images[0].filename %>" class="productImg"
                                alt="<%= product.name %>">
                        </div>
                        
                    </div>
                   
                </div>
                
                
                <!-- <div class="d-flex">
                    <div class="d-flex align-items-center justify-content-center imageDiv">
                        <img src="/images/<%=product.images[0].filename %>" class="productImg"
                            alt="<%= product.name %>">
                    </div>
                    <div class="d-flex align-items-center justify-content-center imageDiv">
                        <img src="/images/<%=product.images[0].filename %>" class="productImg"
                            alt="<%= product.name %>">
                    </div>
                    <div class="d-flex align-items-center justify-content-center imageDiv">
                        <img src="/images/<%=product.images[0].filename %>" class="productImg"
                            alt="<%= product.name %>">
                    </div>
                </div> -->
            </a>
            <div class="product-image-zoom">    

            </div>
            <div class="card-body d-flex flex-column">
              <div class="d-flex align-items-end"> <h3 class="card-title">
                <%= product.productName %>
           </h3></div>
                <p class="card-text"> <h5><b>$<%= product.price %></b></h5>
                </p>
                <p class="card-text"> <h5><b><%= product.productCategory.name %></b></h5>
                </p>
                <p class="card-text"> <h5><b><%= product.productSubcategory.name %></b></h5>
                </p>
                <p class="card-text description"><%= product.description %>
                </p>
            </div>
        </div>
        <script src="/javascripts/user/productZoom.js"></script>
    </body>

    </html>